import React from 'react';
import PButton from '.';

export default {
  title: 'PButton',
  component: PButton,
};

export const component = () => <PButton>hello</PButton>;

component.story = {
  name: '버튼',
};
